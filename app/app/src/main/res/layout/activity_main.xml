<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/refreshAuditRules"
        android:layout_width="42dp"
        android:layout_height="45dp"
        android:layout_marginStart="284dp"
        android:layout_marginTop="8dp"
        android:onClick="refreshAuditRuleList"
        android:text="↺"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/addAuditRule"
        android:layout_width="45dp"
        android:layout_height="44dp"
        android:layout_marginTop="8dp"
        android:onClick="addAuditRuleBtn"
        android:text="+"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteX="326dp" />

    <ExpandableListView
        android:id="@+id/rulesView"
        android:layout_width="368dp"
        android:layout_height="450dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/refreshAuditRules" />

</android.support.constraint.ConstraintLayout>
